# 编辑录入与发布流程

> 当前为开工前v0.2草案。第一阶段为D1～D25约25个工作日；D25综合验收前只录代表样本，不进行全量导入。

## 1. 目标

- 编辑人员可以使用非管理员账号独立录入商品、文章、页面、图片和SEO信息。
- 开发人员可以使用真实内容验证前端，不需要等待所有视觉素材完成。
- 用约25个工作日验证数据、权限、媒体、URL和发布规则，再开放持续批量录入。

## 2. 环境和权限

- 录入环境：受密码保护并设置`noindex`的Staging。
- 禁止在LocalWP和Staging使用真实支付。
- 编辑账号不得安装插件、修改主题、调整固定链接或管理其他管理员。
- 当前没有独立SEO岗位。Website Manager负责内容级SEO与Yoast元数据的日常操作；系统不额外阻止已发布Slug、Canonical或robots变更，但这些高影响操作必须先记录原因、目标URL和影响范围，再由开发者复核技术SEO、URL和回滚影响。
- 商城管理员权限只授予确需处理订单、优惠券或库存的人。

### D5试录权限基线

- `DentAll Content Editor`只用于D6安全试录：可新建和编辑自己的文章、商品草稿，可上传JPEG、PNG和WebP图片。
- 试录账号不得发布文章或商品，不得编辑他人内容，不得创建页面、管理全局商品分类/属性，也不得访问订单、优惠券、WooCommerce设置、用户、插件、主题或站点设置。
- D11验证后，试录图片单文件上限继续保持5MB，且仅允许JPEG、PNG和WebP；SVG、PDF和其他文件类型继续关闭。后续只有确认公开PDF需求并完成Website Manager最小权限与安全测试后才单独开放；媒体公开URL不得保存敏感资料。
- SEO插件已确定为Yoast SEO Free。Website Manager保留`wpseo_edit_advanced_metadata`，可见内容级SEO与Advanced字段；按用户决定不增加代码限制，Canonical、robots和单页索引调整依靠流程、培训与回归控制。
- WooCommerce原生`Shop manager`权限覆盖订单和商城设置，未确认实际商城管理员前不分配给试录人员。

### 统一业务所有权角色（商品与商城运营权限已完成Local首轮验证）

| 角色 | 业务所有权 | 可独立完成 | 必须升级处理 |
|---|---|---|---|
| `DentAll Website Manager` | 网站业务内容与商城日常运营 | 使用独立账号管理文章、页面、媒体元数据、分类、标签、评论/商品评价、内容级SEO和发布，以及商品、属性项、品牌、价格库存、变体、订单、优惠券、报表、客户创建和WooCommerce日常设置 | 真实支付/退款、批量高影响操作、大量已发布Slug或层级变化、固定链接、全局SEO、插件主题、WordPress用户、数据库和部署按变更流程升级 |
| `DentAll Content Editor` | 被分配的日常录入 | 按既有结构录入草稿和素材 | 新建结构、发布或跨作者修改按负责人流程升级 |
| `Administrator` | 技术骨架与系统管理 | 权限、代码、插件、主题、系统设置、URL机制、备份恢复和部署 | 不替业务方判断商品事实、正式分类归属、价格库存或内容真实性 |

- “业务所有权”表示业务负责人不需要开发者代建每一个商品、分类、文章栏目或标签；开发者只处理技术能力、系统边界、URL影响、安全和异常修复。
- 商品与文章分类均由同一个Website Manager角色日常维护，工作人员无需切换角色，也不需要开发者逐项代建。多人参与时每人使用独立账号并分配该角色，不共享登录凭据。
- Website Manager仍不是Administrator或原生Shop Manager的直接复制：采用项目白名单开放WooCommerce运营能力，不开放`manage_options`、WordPress用户管理、插件、主题、核心更新、数据库和部署。
- 商品、文章、页面、媒体元数据、评论、属性、优惠券、订单入口、客户和报表已在D12完成Local与Staging验证。WM-A已使用该角色实测；依据CR-007，D24-D25由WM-A代表Website Manager角色完成当前验收，WM-B跨账号和两人培训改为特殊情况触发时补验，不属于当前门槛。`DentAll Content Editor`保留为未来可选角色，暂不作为当前人员验收要求。
- Website Manager不需要访问WordPress“插件”页面。后续Site Kit由Administrator安装和首次连接，再通过Dashboard Sharing向Website Manager共享只读数据；GTM标签日常管理在Google Tag Manager平台单独授权，WordPress端容器接入和全局追踪配置仍由开发者处理。
- 当前`manage_woocommerce`会开放WooCommerce日常设置能力；这不等于可以无流程修改支付、税费、物流、邮件、Webhook或Production配置。相关操作继续要求变更记录、测试、备份和开发协同。
- Website Manager拥有Yoast高级元数据权限不等于可以无记录修改索引。SEO Title和Meta Description属于日常内容操作；已发布Slug、Canonical、是否索引、nofollow及高级robots属于高影响操作，保存前应记录旧值、新值、原因、受影响URL和复核人。

### 已发布Slug与Canonical操作

1. 草稿或从未公开的TEST对象可在首次正式发布前修正Slug，不为无公开价值的测试URL创建Production重定向。
2. 已发布URL需要修改时，Website Manager先记录旧URL、新URL、原因、页面状态、负责人和回滚方案；记录完成前不保存新Slug。
3. 永久迁移使用一跳301，目标必须是内容和搜索意图一致的200页面；不得把多个不相关商品统一跳首页、商店或分类页。
4. Canonical只用于仍需同时访问的重复或高度相似页面，不用于替代301，也不用于把停售商品权重强行指向不等价商品。
5. 修改后由Website Manager检查内容、搜索意图与内链并记录业务原因，开发者核对状态码、循环/链条、Canonical、robots、Sitemap和缓存；证据写入`URL_SEO_MAP.md`。

### 临时缺货与永久停售操作

1. 临时缺货：Website Manager更新可信库存为0或`outofstock`，保持发布、原Slug/URL、目录可见和禁止Backorders；核对前台显示不可购买。没有确认日期时登记下次复核时间，不把不确定状态当成永久停售。
2. 永久停售：必须先取得业务方确认，记录最后可售日期、售后/资料保留需求、候选替代商品和负责人；不得直接删除、改Slug、复用SKU或只把库存设为0后结束流程。
3. 业务负责人确认候选替代在产品用途、售后和客户承诺上是否成立；Website Manager记录用户意图、内链和内容价值依据，开发者结合流量、外链、状态码和索引影响复核技术处置。同分类商品不能自动视为严格替代。
4. 严格替代走301；有持续价值走原URL 200停售页；无替代且无价值走真实404，410仅在有明确永久删除需求时单独评估。
5. Website Manager可以执行已批准的商品内容和库存操作；301、状态码、结构化数据与Production缓存/部署由开发者按变更流程实施和验证。

## 3. 第一阶段开放门槛

| 阶段 | 时间 | 允许操作 | 禁止操作 | 通过标准 |
|---|---:|---|---|---|
| 安全试录 | D1-D6 | 保存1个简单商品和1篇文章草稿 | 批量导入、正式发布、调整系统配置 | 非管理员账号可独立完成且无P0/P1阻塞 |
| 数据原型 | D7-D12 | 建分类、属性和2～3个商品原型 | 全量商品导入、一次性创建大量属性 | 数据来源、字段和媒体规则草案可执行 |
| 商品样本 | D13-D18 | 录入5～10个代表商品 | 推进全量商品、随意增加核心字段 | 简单/变体/促销/缺货/多图等样本通过检查 |
| 内容样本 | D19-D24 | 录入3篇文章和1个页面 | 大量建分类、改变URL结构 | 草稿、预览、审核、媒体和SEO流程通过检查 |
| 综合开放 | D25 | 处理遗留P0/P1并冻结v1规则 | 未验收就批量生产 | 权限、字段、URL、导出、备份和恢复路径通过 |

D6开始试录；D18形成商品模型候选冻结；D24完成文章与页面样本；D25验收后开放批量录入。

### D12最小商品原型

1. 创建一个固定包装的TEST Simple Product，验证唯一SKU、价格/促销、按销售包装计数的库存、Package Quantity及当前`lbs/in`下的TEST物流字段。
2. 创建一个2个Size×2个Shade的TEST Variable Product；只建立3个合法Variations，保留1个非法组合，覆盖Variation SKU、同价/异价、独立库存、缺货、父级物流继承和一个Variation物流覆盖。
3. 只有D12确认需要立即验证`display_only`销售模式和参考价格字段时，才创建第3个TEST Display Only Product；否则留到CR-005实施阶段。
4. 原型名称、SKU、属性值、价格、库存和物流数值都必须明确标记TEST；不借用HP0103G、FG0312D、运动护齿/EVA或锆块的未知正式业务事实。
5. D12只创建原型实际使用的最小属性项；用户已决定保留Local与Staging的TEST对象作为D13及下周回归夹具。它们不得直接转成正式商品，D25开放批量录入前再次决定归档或清理。

## 4. 商品代表样本

第一批5～10个商品至少覆盖：

- 简单商品。
- 可变商品及不同变体价格或库存。
- 正常价和促销价。
- 有货和缺货。
- 单图和多图图库。
- 重量、尺寸或物流信息。
- PDF资料或认证文件（如果第一版确认需要）。

每个样本检查：名称、Slug、SKU、类型、价格、库存、分类、属性、简短描述、详细描述、主图、图库、SEO标题和描述。

### D13简单商品发布字段候选

以下是D13 Website Manager预演后形成的候选，不代表D18商品模型已经冻结：

- 创建草稿即可暂存名称、描述和未齐资料；标准可购买商品发布前必须补齐英文Slug、唯一SKU、正数常规价格、库存状态、至少一个商品分类、简短描述、详细描述、主图及主图Alt。
- 品牌作为DentAll业务发布必填候选：使用`ADS`、经确认的原厂品牌或正式确认的无品牌表达，不使用`Unknown`；品牌归档URL和筛选实现仍按后续计划冻结。
- 启用数量跟踪时必须填写可信库存数量；Backorders默认不允许，例外需要业务确认。没有可信数量时不能填写虚假大数，可按已批准流程仅维护库存状态。
- 促销价格与日期、图库、重量尺寸、属性、GTIN、关联商品、虚拟和下载设置均按商品实际条件填写，不为填满后台而编造。
- WooCommerce技术上允许SKU留空，但DentAll只允许资料未齐的草稿暂时留空。标准Simple、Variable父商品和每个可售Variation正式发布前都必须具备来源明确且全站唯一的SKU；无合适制造商货号时由Website Manager分配DentAll自有SKU。

### D14可变商品操作候选

1. 先确定客户必须选择且会改变SKU、价格、库存、图片或可购买性的属性；只展示或筛选的属性不自动用于Variation。
2. 计算理论组合并取得合法组合表，只创建真实可售组合；D14 TEST矩阵为2个Size×2个Shade，但只保留3个合法Variations。
3. Variable父商品保留系列级唯一SKU，父级价格由可售Variations派生。每个可售Variation分别填写唯一SKU、正数Regular price、库存真相源、Backorders状态及物流继承/覆盖结论。
4. 独立库存模式下，父商品不启用数量库存，由每个Variation维护数量；只有全部Variations确实共享同一物理库存池时才改用父级库存，父子不得同时管理数量。
5. 没有真实默认销售组合时，Size和Shade默认值保持为空。需要默认值时，只能指向合法、启用、有有效价格且可购买的Variation，并在前台验证价格、库存、图片和加购状态。
6. 前台至少验证一个有货组合、一个缺货组合、一个未建立的非法组合及一个异价组合；加购后检查购物车中的属性、数量和金额，并清理TEST会话。
7. 物流字段为空代表继承父值，不等于未知数据已确认；真实物流不同的Variation明确覆盖。D15～D18已验证TEST继承/覆盖机制，正式单位、测量对象和数值仍由业务与后续物流方案确认。

### D15库存与物流操作候选

1. 录入物流前先确认测量对象：产品本体规格进入属性/内容；一个购物车销售单位连同直接包装的发货重量和Length×Width×Height进入Shipping；最终订单外箱不录入单个商品字段。
2. Shipping字段只填纯数值，单位读取当前WooCommerce全站设置。当前Local为`lbs/in`，但承运商、物流插件和真实样本未定，不能复制TEST值作为正式商品数据。
3. Simple在商品自身维护物流数据。Variable各Variation实际相同时可留空继承父值；实际不同时明确覆盖。留空前必须确认父值适用，不能用继承掩盖未知数据。
4. 有可信可售数量时启用数量跟踪；只能确认能否销售时关闭数量跟踪并维护库存状态。两种模式切换后必须保存、重新打开并核对前台，恢复数量模式时显式重填可信数量和Backorders。
5. 第一版默认禁止Backorders。临时缺货时保持已发布商品的URL和Slug稳定，设置`outofstock`并确认前台不可购买；是否全站隐藏缺货商品不得由单个编辑自行决定。
6. 永久停售不直接删除、不自动改Slug，也不把“库存为0”当成完整停售方案。记录业务确认、替代商品、历史流量/外链和订单引用，交给D16决定页面文案、301、Canonical与索引策略。
7. 每次库存测试只操作明确TEST商品，记录ID、SKU和恢复基线；结束时后台重开、WooCommerce CRUD和前台三方确认，购物车清空。不得用该流程创建真实订单或触发支付、税费和正式物流。

### 定制展示商品流程（已确认，待实现验证）

1. 商品继续使用WooCommerce Product和唯一详情URL；编辑仅在业务确认该内容用于展示而非直接销售时启用`display_only`。
2. 编辑可填写经业务确认的参考价格下限和上限；两者是非交易USD展示值，未确认时留空，不得从仓库价格或示例值推测。
3. 前台显示明确标注的参考价格范围，但不显示普通成交价、库存、数量、加购或立即购买，也不进入结账、库存扣减和购买事件。
4. 商品详情提供`Contact Us About This Product`，进入通用联系流程并携带Product ID；Contact页向客户显示当前咨询商品，服务端重新读取商品上下文。
5. Contact提交沿用统一表单记录、邮件、反垃圾、隐私和失败处理，并标记`source=custom_product`；不自动创建报价或WooCommerce订单。
6. 如果未来出现完整报价需求，先将CR-004重新评估为Ready，再实现独立询价记录、权限、人工报价转订单和完整测试。

## 5. 文章与页面代表样本

- 1篇短文章，检查摘要、特色图和列表展示。
- 1篇长文章，检查H2/H3层级、图片和目录需求。
- 1篇包含商品或分类内链的文章。
- 1个固定页面，例如About或政策页。

禁止从Word或其他网站直接粘贴不可控的字体、颜色和内联样式。图片上传前完成文件名、尺寸、压缩、授权和alt检查。

### D24原生Page草稿、状态流转、缓存与导航操作手册

本节依据2026-08-21至2026-08-22在受保护Staging对Page #76的实际界面复演与C7抽样编写，优先服务Website Manager培训。它覆盖“建立TEST草稿、回读、预览、待审、发布、撤回、缓存清理、受控导航和修订恢复”的WM-A角色级路径；完整逐步手册、无需背诵项、必须理解项、停止条件和C7证据见[[Day24-内容发布操作手册与WM-A培训]]。CR-007已将WM-B重复角色验收移出D24-D25当前范围。第二身份、跨账号归属和多人协作没有通过，只有触发特殊情况时才补验；A/C5培训、A/C6 SOP辅助人员验收和A/C7状态/字段/预览/隔离/修订/登记抽样均已通过，但正式3篇文章、业务Page与授权16:9素材仍未验收，因此不能据此声称D24整体完成。

#### 账号与前置条件

- 使用工作人员本人独立的`DentAll Website Manager`账号；Administrator只负责创建账号和系统级维护，不能代替Website Manager完成培训证据。
- 当前只使用已验证的WM-A，不为凑验收创建WM-B。未来第二位人员实际需要账号时，WordPress原生“添加用户”界面要求有效邮箱；必须使用公司控制、唯一且可收信的邮箱或已验证可收信的公司别名，不得留空、填写虚构地址、共享WM-A邮箱/账号或把同一账号改显示名冒充第二人。
- 登录后先核对左侧菜单：Website Manager可以看到文章、媒体、页面、评论及已授权的WooCommerce运营入口，但不应看到外观、插件、用户、工具和WordPress全局设置。
- 只在受保护Staging使用标题含`TEST`的对象；不要为演练创建虚构的正式Solutions页面，不修改Reading设置、固定链接或Production。菜单只按本手册建立受控Staging测试菜单，不把它视为最终站点导航。

#### 建立并回读Page草稿

1. 进入“页面 → 添加页面”，输入明确TEST标题。D24夹具为`TEST D24 Page Field Walkthrough`。
2. 正文从普通Paragraph开始；页面标题由模板输出唯一H1，正文不要再插入H1。D24夹具随后使用一个H2 `Scope`和一个三项List。
3. 点击“保存草稿”，不要点击“发布”。只有地址栏出现对象ID且页面显示最近编辑时间，仍应继续通过右侧“页面”面板回读，不以自动草稿代替人工保存。
4. 在右侧“页面”面板核对状态、Slug、作者、模板、父页面和评论。D24 Page #76的实测值为Draft、`test-d24-page-field-walkthrough`、作者WM-A、默认模板、无父页面、评论关闭。
5. 首次发布前可以修正尚未公开的TEST Slug；一旦URL曾公开，必须按已发布URL变更流程记录旧值、新值、原因、影响范围与回滚方案。

#### 预览、匿名隔离与Sitemap

1. 点击编辑器顶部的预览图标，选择桌面并在新标签页预览。登录态预览URL是临时工作链接，不进入正式内链、菜单或对外沟通。
2. 检查页面标题只输出一个H1，正文Heading层级正确，列表仍为语义化List。当前主题尚未完成时只验字段和结构，不把临时主题视觉当作最终四端验收。
3. 使用未登录WordPress的无痕窗口访问不带`preview=true`的对象URL或`?page_id={ID}`。Draft应返回404或其他明确不可见状态；若匿名访客看到正文，按P0停止后续操作。
4. 在无痕窗口打开`page-sitemap.xml`并搜索该Slug。Draft不得出现；D24 Page #76的实测结果为`0/0`。

#### 待审、首次发布与非强制互审

1. 完成发布检查单后，把从未发布的Page从`Draft`改为`Pending Review`并保存；随后匿名访问精确URL和`?page_id={ID}`，两者均应不可见，`page-sitemap.xml`也不应包含该Slug。
2. `DentAll Website Manager`角色具备发布能力。D24中，WM-A把自己创建的Page #76送审后仍能看到并使用“发布”按钮，也能由同一账号完成发布。这证明`Pending Review`只是协作信号，不是系统强制的互审闸门。
3. CR-007下由WM-A完成自检和当前发布流程；不得把“显示待审”写成“系统已经阻止作者自行发布”。若以后要求作者不能自审自发，或第二位人员需要承担独立复核责任，需重新登记并评估账号、角色拆分或工作流能力。
4. 首次发布后使用未登录窗口检查精确固定链接返回200、正文可见，并确认`page-sitemap.xml`包含该Slug。只看到编辑器“已发布”提示不算完整验证。

#### 撤回、缓存清理与精确URL验证

1. 误发布、授权撤回或事实失效时，将对象从`Published`改为`Draft`或按批准方案设为`Private`，并立即记录对象ID、原URL、原因、执行人、时间和后续方案。
2. 撤回后先确认Sitemap不再包含该Slug，但**不能只看Sitemap就判定已下线**。D24实测中，Page #76已恢复为Draft且Sitemap已移除，精确固定链接仍被Breeze/Varnish旧页面缓存以HTTP 200返回，响应为`X-Cache: HIT`；这按公开内容仍可访问的P0处理。
3. 在Staging执行Breeze的`Purge All Cache`，并在可用时同步清理Varnish。随后重新请求**不带随机查询参数的精确原URL**，必须返回404或批准的不可见状态；本次复测为HTTP 404、`X-Cache: MISS`，P0才关闭。
4. 随机查询参数可以帮助判断是否命中旧缓存，但不能替代精确原URL复测。恢复发布后页面被正常缓存而出现`X-Cache: HIT`并不等于故障；判断依据是当前内容状态与响应是否一致。D24 A/C3阶段曾重新发布Page #76并验证精确URL为200，后续Published页面的`HIT`属于预期；A/C4结束时已按本手册后文收口为Draft，最终应以404且无正文泄漏为准。

#### 受控菜单与Storefront fallback

1. 发布Page前先到“外观 → 菜单”确认Primary与Handheld均已显式分配菜单。Storefront在这两个位置未分配菜单时会回退到页面列表，所有已发布Page可能自动出现在桌面和手持导航中；这与“自动将新的顶级页面添加至此菜单”复选框不是同一机制。
2. D24在Staging建立原生菜单`TEST Staging Controlled Navigation`（菜单ID 29），顺序为Home、Blog、Cart、Checkout、My account、Shop；同时分配给`Primary Menu`和`Handheld Menu`。
3. “自动将新的顶级页面添加至此菜单”保持未勾选，`Secondary Menu`不分配。测试菜单不加入Sample Page、D12 TEST Page或Page #76；最终正式导航仍按后续信息架构和四端导航工作日确认。
4. 发布或重新发布Page后，同时检查桌面Primary和Handheld两份菜单HTML/前台入口；精确URL200和Sitemap收录正常时，该Page仍不应因发布状态自动进入这两个菜单。

#### 状态动作登记

- `Pending Review`、首次发布、撤回为Draft、缓存清理、受控菜单配置和重新发布均写入`CONTENT_ASSET_REGISTER.md`的“内容发布与变更登记”；至少记录对象ID与URL、动作摘要、前后状态、执行人、复核人、业务确认人、证据位置、时间、撤回/恢复方案和验证结果。
- Markdown登记簿是当前唯一活动业务留痕载体，不替代WordPress修订、HTTP状态、Sitemap、菜单和缓存复测；`post_author`和修订记录也不能反推出谁执行了发布、撤回或清缓存。早期`.xlsx`中的D24 A/C3-A/C4记录已迁移，生成文件已删除且不再作为事实来源。

#### A/C4已发布更新、修订与两类恢复（Staging实测通过）

以下步骤只用于受保护Staging的明确TEST Page #76。每一步确认当前状态和公开面后再继续；不得在Production照抄演练。

1. 建立Published基线：后台确认状态为`Published`，记录标题、Slug、作者和修订数；以无参数精确URL确认HTTP 200、Page Sitemap包含Slug、Primary与Handheld导航均不含#76。
2. 低风险已发布更新：正文末尾新增独立段落`TEST C4 low-risk update marker — 2026-08-21.`，只点击一次“更新”。预期状态仍为`Published`且公开页立即显示新段落；标题、Slug、作者、模板、Yoast和菜单不变。若随机参数能看到新内容而无参数精确URL仍显示旧内容，清理Breeze/Varnish后必须用无参数URL复验。D24本次已实测通过：更新后修订数为3，无参数精确URL显示标记并返回200/MISS，Sitemap仍含，Primary与Handheld均不含#76。
3. 修订恢复：把该段落改成`TEST C4 temporary revision V2 — 2026-08-21.`并更新，先确认公开页显示V2；再进入原生“修订版本”，选择并恢复包含第一条标记的上一版本。预期页面仍为`Published`，公开页恢复第一条标记且V2消失；同时检查修订账号/时间，标题、Slug和作者不得意外改变。修订恢复会直接更新公开对象，不会生成待审副本。D24本次已实测通过：V2保存后修订数3→4；恢复WM-A的revision #85后修订数4→5，状态继续Published，公开URL恢复V1且V2消失，标题、Slug、作者、Sitemap和导航边界不变。
4. 普通回收站恢复：从`Published`移入回收站后忽略页面提示条中的“立即撤销”，立即清Breeze/Varnish；确认对象在回收站、无参数精确URL为404且`X-Cache: MISS`、Sitemap与导航无#76。随后到“页面 → 回收站”点击普通“恢复”。本次要验证其恢复为`Draft`而不是自动公开；精确URL继续404，Sitemap与导航继续无#76。D24本次已实测通过：#76从Published进入Trash并清缓存后精确URL为404/MISS；普通恢复后出现在Draft列表，精确URL仍404/MISS，Sitemap与两份导航均无#76，不会自动重新公开；重新打开后V1、5个修订、标题、Slug、作者和模板均保持，V2仍不存在。
5. “立即撤销”路径：把上一步的Draft按发布检查重新发布，先确认URL 200、Sitemap包含且导航不含；再次移入回收站，并只点击当前页面提示条里的“立即撤销”。本次要验证它是否恢复移入前的`Published`状态并立即重新公开。该按钮不得作为普通安全恢复的同义词。D24本次已实测通过：#76从Published进入Trash后立即撤销，后台与REST都恢复为Published，精确URL立即200并显示V1，Sitemap重新包含，两份导航仍无#76。培训中必须把该动作标为“可能直接重新公开”，不能与普通恢复到Draft混用。
6. 最终收口：立即把#76改回`Draft`，强制清Breeze/Varnish；最终以无参数精确URL返回404且无正文泄漏、Page Sitemap不含Slug、两份导航不含#76作为完成门槛。D24本次已实测通过：后台为Draft，V1与5个修订保留、V2不存在；匿名精确URL为404，REST为401，Sitemap与导航均无#76。清理后的首轮404随后命中404页面缓存并显示`X-Cache: HIT`、`Age: 0`仍属安全结果；真正的失败条件是旧Published正文仍以200返回，不能把“永远必须MISS”误写成完成条件。
7. 把低风险更新、V2更新、修订恢复、首次移入回收站、普通恢复、重新发布、第二次移入回收站、立即撤销、最终撤回与缓存清理逐项写入“内容发布与变更登记”。D24已新增10条C4记录，连同C3共16条内容记录；本轮未使用媒体，不新增虚构素材记录。

缓存判断：Published正文更新或修订恢复只在无参数精确URL与当前内容不一致时清缓存；`Published → Draft`或`Published → Trash`必须清Breeze/Varnish；`Draft → Trash`或普通恢复到Draft时，若无参数精确URL已经稳定为404可不重复清理。清理后的首次证据优先保留404/MISS或Age 0；后续404被页面缓存命中并不代表泄漏。任何200、旧正文或状态与Sitemap持续不一致都必须立即停止并清缓存。

停止条件：Draft、Trash或普通恢复后经清缓存精确URL仍200，普通恢复意外成为Published，或发生内容丢失，均按P0停止；修订恢复错误版本/状态、清缓存后已发布更新仍不生效、#76进入导航或Sitemap与状态持续不一致，按P1停止。先恢复到Draft、清缓存并保留证据，再继续排查。

#### 系统权限负向检查

- 菜单不可见只是第一层证据。培训或角色变更后还要直接访问`users.php`、`plugins.php`、`themes.php`、`options-general.php`和`tools.php`，五类入口均应拒绝。
- 任一Website Manager能够列出用户、管理插件/主题、进入工具或修改WordPress全局设置时，按P0停止；不要继续发布或通过隐藏菜单掩盖越权。
- 本检查不收集或共享密码。截图只保留账号显示名、地址栏、菜单与结果，邮箱和其他个人信息按需遮盖。

#### WM-B条件性补验（CR-007，不属于当前D24-D25）

1. 只有第二位人员正式上岗、角色或界面表现不一致、需要跨账号修订归属、强制互审/审计、交接、多人同时维护登记簿、账号级插件/MFA/通知差异或角色升级回归时，才启动本节。
2. Administrator用公司控制、唯一且可收信的邮箱创建真实WM-B；WM-B本人独立登录，不把凭据交给其他人，也不共享WM-A。
3. 使用明确TEST且已确认匿名不可见的Draft Page，由WM-B只追加一个正文标记并保存草稿；不得在Published对象上用“更新”代替跨账号草稿测试，不得改派作者、修改Slug、SEO高级字段或发布。
4. WM-A重新登录，确认标记持久化、作者未意外改派，并在原生修订中核对WM-B账号与时间；WM-B同时重复菜单与五类直接入口负向检查。
5. 如涉及多人维护Markdown登记簿，还要验证公司控制的Git远程、分支/合并冲突处理、备份与交接。完成前只能写“条件性补验进行中”，不能写成双账号或多人验收通过。

### 5.1 文章作者与审核规则

- 正式文章前台统一公开署名为`DentAll Editorial Team`。Website Manager独立登录账号用于权限控制；`post_author`表示内容作者归属，受支持内容字段的每个修订版本记录对应修改账号和时间。三者都不能单独证明是谁执行了发布、撤回或移入回收站，也不直接等同于前台署名。
- 若CR-007触发且未来第二位Website Manager正式上岗，两人使用各自独立账号直接编写、审核和发布，不新增品牌作者账号或新角色，不共享凭据，也不把两人的后台显示名改成相同团队名称。前台模板与SEO输出负责统一品牌署名。
- 第一版文章只承载工厂、公司、产品与服务相关信息，不需要临床专家署名；工厂数据、资质、材料、产能、交付和品牌表述仍须由业务负责人核实。
- 作者归档关闭；文章正文保留可见署名，前端后续优先链接到About或编辑说明页面。
- 标签为可选组织工具，第一版标签归档统一`noindex`。不要为关键词覆盖创建大量近义标签；新增标签前确认它能被多篇文章复用且不与分类重复。
- 分类是第一版博客的主要归档维度。只有正式名称/Slug、包含多篇相关文章并具备独立说明的分类才能作为可索引页面；`Uncategorized`、TEST、空分类和单篇薄弱分类不得进入Production导航或内容验收。
- 如果文章出现临床建议、法规解释、安全操作、诊疗判断或需要专业资质背书的声明，必须停止普通发布流程并升级给业务/合规负责人，重新决定具名作者、审核人和证据要求。

### 5.2 D20文章录入模板v1

- D20采用WordPress原生Post、区块编辑器和Yoast内容级字段，不新增ACF、CPT、区块插件或保存Hook。WordPress允许不完整草稿；“送审条件”由操作清单判断，不在D20用代码阻止保存。
- 标题负责页面主题并由模板输出H1；正文不得再次插入H1。正文区块骨架、H2/H3关系和允许的常用区块在C2冻结。
- 摘要使用1～2句独立概括，不机械复制正文首段；Meta Description与摘要职责分开，代表样本需要分别保存和验证。
- 文章特色图基线采用16:9，推荐源图1600×900；格式、压缩、文件名、授权和alt继续遵循媒体规范。草稿早期可暂缺，送审前必须补齐。
- 正式送审内容至少使用一个正式主分类，不使用`Uncategorized`、TEST或空分类；标签继续可选，不为关键词覆盖创建一文一标签。
- 有真实相关目标时使用描述性锚文本添加文章、商品或分类内链；不得链接草稿预览、`wp-admin`或临时Staging专属地址。没有相关目标时不强造链接。
- 正常文章的Yoast Canonical和robots保持默认；Website Manager虽有高级字段权限，手工覆盖仍需记录和复核。
- 后台`post_author`保留实际Website Manager账号作为内容作者，受支持字段的修订保留对应修改账号；第一版不新增品牌作者账号，也不把这些记录误当成发布状态操作日志。前台`DentAll Editorial Team`署名及作者Schema转D87实现。

#### 草稿与送审条件

| 状态 | 允许情况 | 不得据此声称 |
|---|---|---|
| 编辑中草稿 | 字段可分阶段补齐；允许TEST标题、TEST分类和未完成正文 | 不等于内容完整、业务事实确认、SEO完成或可以发布 |
| 达到送审条件 | 标题、首次发布前Slug、摘要、正文、特色图/alt、正式分类、后台实际作者和状态自检完成；相关内链与SEO字段按规则检查 | 不自动改变WordPress状态，也不替代D23审核发布治理和D24真实内容验收 |
| 已发布 | 由D23发布流程和业务确认控制 | 不能仅因Website Manager具备发布能力就跳过事实、版权、URL和SEO复核 |

#### C2正文区块骨架

文章标题由WordPress模板输出唯一H1，编辑器正文从引导段落开始，不再插入H1。正文结构使用下面的可复制骨架；括号中的说明是编辑提示，不是需要原样发布的英文小标题。

1. 引导段落：用1～2个段落说明主题、读者和文章能解决的问题，不单独添加“Introduction”标题。
2. H2主题段：交代背景、对象或问题边界。
3. H2主体段：展开主要信息、步骤或说明；确有下级关系时使用H3，H3后返回同级H2。
4. H2实践段：说明应用、选择条件、证据或常见问题，可使用列表和一张有真实信息价值的正文图片。
5. H2结论段：总结关键判断并给出合理下一步；相关内容、商品或分类以内文描述性链接连接，不建立孤立的“Click here”区块。

- 普通文章根据主题使用2～4个H2，不为了凑结构制造空段落；没有真实下级内容时不用H3。
- D20代表长文TEST夹具使用约1200～1500个英文单词、4个H2、至少1个H3、一个无序列表、一个有序列表和一张正文图片。它只验证编辑器、预览和长内容结构，不是正式文章最低字数，也不是SEO排名规则。
- 正文图片保持内容所需的真实宽高比，不强制套用特色图16:9；使用WordPress图片区块和合适派生尺寸，alt按当前语境填写，Caption只在来源、比例、条件或补充说明确有公开价值时使用。

#### 区块分级

| 级别 | 区块/做法 | 使用规则 |
|---|---|---|
| 编辑可直接使用 | Paragraph、Heading（H2/H3）、List、Image、Separator | 第一版正文默认集合；内容必须有语义，Separator不能代替标题层级 |
| 有真实内容时使用 | Quote | 必须是可核实且获准公开的真实引用，注明来源；不得把普通强调文案伪装成引语 |
| 使用前复核 | Gallery、Table、File、Buttons、Video/Embed、Columns、Cover、Group | 分别涉及裁切、移动端表格、公开文件、设计/事件、隐私性能或响应式布局；出现真实需求后按页面和状态验证，不作为D20长文夹具必选项 |
| 第一版普通编辑禁止 | H1、Spacer、Custom HTML、未知Shortcode、直接iframe/script/style、用空段落/连续换行排版、从外部文档粘贴字体颜色和内联样式 | H1由标题负责；布局由主题/CSS负责；自定义代码和第三方嵌入必须走开发、安全、隐私与性能评估 |

#### 标题层级与长文目录边界

- 标题表达内容层级，不用于把文字“放大加粗”。H2下才能出现H3；第一版常规文章不使用H4，确有三层以上知识结构时先复核是否应拆成多篇文章。
- 标题文字必须具体、非空且在同一篇文章中可区分；不得从H2直接跳到H4，也不得用粗体段落伪装标题。
- 第一版不因为文章长就自动安装目录插件，也不手工维护容易失效的目录。正式文章同时具有4个以上H2且读者确有跳转需求时，再在D87文章详情阶段评估可访问、可维护且与稳定Heading anchor一致的目录实现。
- D20长文夹具会暴露目录需求，但保持草稿且不实现目录；这不阻塞正文结构、预览和修订测试。

## 6. 发布流程

### D23第一版状态与职责

| 状态 | 允许动作 | 责任与留痕 |
|---|---|---|
| Draft | 创建、编辑、预览、补充素材与事实 | `post_author`表示内容作者；受支持内容字段的修订记录保留对应修改账号和时间 |
| Pending Review | 完成自检后标记为等待业务确认；未来存在第二人时可按需互审 | 记录待确认事项、确认人及依据位置；未确认素材保持`待核验`或不上传公开媒体库；当前同一WM-A仍可发布 |
| Publish | 新内容通过发布检查单后发布；已发布内容按下方更新分支处理 | 执行人必须使用本人Website Manager账号；发布/同步确认人和时间另行登记，不能从`post_author`或内容修订反推 |
| Draft/Private（撤回） | 误发布、授权撤回或事实失效时立即下线 | 另行记录对象ID、原URL、原因、执行账号、时间和替换计划；已形成公开资产时按URL变更流程处理 |

`DentAll Website Manager`角色具备发布能力；WM-A已实测在`Pending Review`状态仍可自行发布。因此`Pending Review`只是协作状态，不是强制审批闸门。CR-007下当前不建立WM-B或要求另一人互审；若业务以后要求作者与审核者强制分离，必须另行登记范围并评估独立账号、角色拆分、工作流实现、兼容性和回滚。

`Pending Review`只适用于尚未发布的内容。文章或页面一旦发布，在原生编辑器中点击“更新”会直接改变公开内容，WordPress核心不提供“保持旧版本在线、让新版本待审”的原生待审修订。已发布内容按以下分支处理：

1. 小型、低风险修改：Website Manager完成自检后直接更新，随后匿名检查公开页面和必要缓存；仍要使用本人账号，高影响字段不适用此分支。
2. 大幅改写或涉及业务声明、授权、已发布Slug、Canonical、robots等高风险修改：先在受保护Staging或明确标记的独立审阅草稿中准备和审核，批准后再同步到公开对象。独立审阅草稿不得进入正式导航、Sitemap或内链，也不得制造可索引的重复公开URL。
3. 如果业务要求公开旧版本保持不变、同时在同一对象上保留真正的待审修订，或要求系统强制职责分离，必须登记新范围并评估角色、工作流或插件方案的兼容性、性能、权限、迁移和回滚。

原生修订主要覆盖Post/Page的标题、正文和摘要等受支持内容字段，并记录对应修订的修改账号与时间；`post_author`表示当前内容作者，不是最后修改人或状态操作人。原生修订不版本化发布状态与作者字段，也不能天然记录是谁执行发布、撤回、移入回收站或恢复；这些状态动作必须通过业务可访问的发布/变更登记另行留痕，或明确接受第一版审计缺口。不得假定分类/标签、特色图、媒体元数据、Yoast字段或其他自定义元数据能随内容修订完整恢复。修订测试应选择正文标记等明确受支持字段，并对未覆盖字段另设检查或恢复路径。

发布/变更登记的最低字段为：对象ID与URL、动作或变更摘要、前后状态、执行人、复核人、必要的业务确认人、证据位置、日期时间以及撤回/恢复方案。2026-08-21起，第一版唯一活动载体为仓库内`CONTENT_ASSET_REGISTER.md`，“内容发布与变更登记”和“素材授权登记”继续保持字段职责分离。D24 A/C3-A/C4已把Page #76的16条实测状态链迁移到该Markdown，早期`.xlsx`生成文件已删除。公司控制的Git远程归属、WM-A持续维护、备份和冲突处理责任仍须在D25开放批量录入前验收；未来多人协作时再按CR-007补验分支、合并与交接。

### D23发布检查单

1. 新内容由Website Manager创建草稿并完成自检；不得共享账号或代替开发者承担系统操作。
2. 当前由WM-A按清单自检标题、描述、Slug、内链、图片alt、分类、来源依据、版权与品牌表达，必要时取得业务负责人确认。未来存在第二位Website Manager时可按需互审；互审始终依靠流程自律，不是系统强制审批。
3. 出现医疗/法规/安全声明、资质、产能、价格、交付承诺或授权不明确的素材时，新内容保持`Pending Review`；已发布内容不要直接在公开对象中写入未批准改动，改走受保护Staging或独立审阅草稿。Website Manager向老板或对应业务负责人取得确认，开发者不替代业务审核。
4. 首次发布或高风险更新前在受保护Staging预览；前端模板未完成时只验内容、字段、原生修订和媒体登记，不验最终视觉。
5. 通过检查后由Website Manager首次发布或同步已批准更新；已发布Slug、Canonical、robots或索引变更仍遵循高影响URL/SEO记录与复核流程。同步后匿名检查公开页面和缓存。
6. 发布、更新、撤回与回收站动作的执行账号、时间、对象ID和依据位置写入`CONTENT_ASSET_REGISTER.md`的“内容发布与变更登记”；不能从`post_author`或内容修订记录反推这些状态动作。D25前还要验证公司控制的Git远程归属、WM-A持续维护、备份与冲突处理责任；多人协作仅在CR-007触发后补验。
7. WordPress原生修订记录仅覆盖受支持的内容字段、对应修改账号和时间，不承诺恢复分类/标签、特色图、媒体元数据、Yoast字段，也不审计所有WooCommerce或后台操作。第一版使用同一Markdown文件中的“素材授权登记”补充业务留痕，由已确认责任人根据真实业务证据维护；已删除的历史`.xlsx`不得重新生成或同步。全量后台审计日志转第二版按需评估。

误发布或授权撤回时，把内容改为草稿/私密只是紧急下线动作。操作后要验证匿名访问并处理缓存，再决定恢复发布、相关301、明确410或其他永久URL方案；不得把无关页面作为重定向目标，也不能让公开URL长期处于未记录的临时状态。

## 7. 反馈与变更

- 后台无法保存、权限错误、字段缺失或数据丢失：P1，进入当前里程碑处理。
- 影响大量商品或URL的字段变化：暂停批量录入，登记需求变更和迁移方案。
- 纯便利性调整、个性化布局或非必要自动化：P3，进入Backlog。
- 编辑反馈应包含账号角色、内容URL/ID、操作步骤、预期结果、实际结果和截图。

## 8. D18商品模型候选冻结

- [x] 5个代表商品骨架完成：D17新建3个并补全既有2个；真实业务内容另行验收。
- [x] SKU、分类、属性、变体、价格和库存职责无冲突；分类内容与正式业务值仍由Website Manager维护。
- [x] 图片比例、文件名、压缩和alt规范可执行；正式素材与授权仍待业务方提供。
- [x] SEO字段和Slug规则可执行；Staging索引与Canonical只验环境边界，Production另验。
- [x] Website Manager培训者无需管理员权限即可完成录入；该证据只覆盖培训者/角色路径。WM-A已按CR-009的SOP辅助人员验收口径通过A/C6；第二身份人员无指导试录按CR-007触发时补验。
- [x] Website Manager可使用WooCommerce原生商品CSV导出；权限仅限商品导出请求，不开放WordPress全站内容导出。Local 5行与Staging 10行核心字段核对通过，中文重复“交叉销售”表头与无损回导仍待D25处置。
- [x] TEST对象去留已形成候选：Local回归夹具保留；Staging代表样本保留至D25；#31为空壳删除候选，#39需先核对历史公开/引用证据；C5～C6未执行删除或状态变化。
- [x] 数据字典已更新并标记D18商品模型候选冻结；M2不等于D25批量录入许可。

## 9. D24内容样本验收

- [ ] 3篇代表文章和1个固定页面完成。
- [ ] 博客分类、标签和URL规则无冲突；`post_author`内容作者、受支持内容字段的修订账号与作者归档关闭通过验证，并明确状态操作需要另行登记。前台统一署名与作者Schema仍按ADR-017转D87实现，不作为D24临时主题验收项。
- [ ] 草稿、预览、审核和发布权限正确。
- [ ] 特色图、正文图片、内链和SEO字段可执行。
- [x] 当前负责操作的WM-A可通过批准的中文SOP判断哪些内容级SEO可自行维护，哪些已发布URL、Canonical、robots、全局模板和系统变更必须先找开发确认；A/C6按该SOP辅助口径通过。未来第二位人员上岗时另做相同培训和条件性补验。
- [x] A/C5中文操作手册、Markdown登记说明和当前问题清单已更新；A/C6按可查SOP口径通过且未补造闭卷证据；A/C7完成四对象状态、Post/Page字段、登录预览、公开隔离、修订恢复和Markdown登记抽样，开放P0/P1为0。

## 9.1 D17代表商品SEO样本结果

- [x] Local与Staging商品固定链接均已由用户设置为`/product/{slug}/`，`/shop/`保留为归档；Staging两个商品和归档回归通过。保存“默认”后刷新回显第四项`product/`是WooCommerce规范化后的预期行为，不需要反复修改。Production未修改；Website Manager后续不自行调整固定链接设置。
- [x] Staging已由Administrator按授权安装Yoast 28.2，并由用户手动激活成功；Website Manager不安装或升级插件。五页只读矩阵已验证Title唯一性和Staging全站`noindex, nofollow`，真实字段编辑流程留给D17代表样本。
- [x] 累计5个代表样本覆盖Simple、Variable、缺货Variation和多图；促销沿用D13 TEST证据，可下载与真实永久停售因无可信输入未验收，不为凑数量编造。
- [x] 5个样本完成名称/H1、Slug、唯一TEST SKU、价格/库存、分类、SEO Title和Meta Description的保存及输出检查；所有未确认内容均保持TEST或草稿，结论为“骨架通过、真实内容未验收”。
- [x] Staging继续保持访问保护、站点`noindex`和真实支付关闭；未把Staging绝对URL写入正式Canonical或对外内容。
- [x] Variable样本2×2理论组合只建立3个合法Variations；有货、缺货、异价、物流覆盖和非法组合不可选均通过前台检查，未执行加购，购物车未改变。
- [x] 样本盘点排除#31空壳草稿与#39历史不完整草稿；D17不删除或修复，D18登记归档/清理候选，D25前决定TEST对象去留。

## 10. D25批量录入开放验收

- [ ] 商品和内容样本的P0/P1问题已关闭。
- [ ] 商品字段、文章分类、URL、图片比例和发布规则标记为v1。
- [ ] 编辑、SEO、商城管理员权限已复核。
- [ ] Staging数据库和uploads已有备份。
- [ ] 商品与文章可以导出，恢复路径经过抽查。
- [ ] 已明确Staging内容最终如何同步到Production，且不会覆盖正式订单和客户数据。
