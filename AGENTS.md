# DentAll WooCommerce 项目规范

## 项目目标

用 WordPress + WooCommerce 重做 DentAll 商城第一版。开发者为单人，当前采用单休拉长版：20周、每周6天、120个工作日，自然周期约4.6个月，对外按4.5～5个月管理。当前第一优先级是用D1～D25建立稳定、安全的后台内容生产线，让编辑人员先验证商品和文章流程，再与前端开发并行推进。目标不是无限定制，而是在可维护、可测试和可上线的前提下完成商城闭环。

## 沟通与文档

- 所有 Markdown 说明文档使用中文。
- 回答先给结论，再给范围、风险和下一步。
- 不把估算表述为无条件承诺。
- 发现需求会影响数据、URL、支付、物流、SEO、缓存或上线时，必须明确提示。
- 新需求必须记录范围与工时变化，不默认吸收。
- 默认以完成任务和给出必要说明为主，不主动展开基础知识教学。
- 当用户明确表示“不懂”、主动询问原理或要求讲解时，切换到详细教学模式：使用记忆宫殿、实体比喻、因果关系和项目中的真实操作路径分层讲解；先建立整体模型，再说明具体步骤、风险和验证方法。
- 详细教学不能用比喻替代准确技术事实；每个比喻后要对应回真实的WordPress、WooCommerce、PHP、数据库或服务器概念。

## 骨架优先与业务确认规则

- 所有后续需求梳理和业务询问，先判断未知信息是否会改变通用骨架。通用骨架包括数据模型、内容类型、字段职责、角色权限、安全边界、URL机制、交易流程、接口契约、动态前端状态和不可逆配置。
- 不影响通用骨架的逐商品或逐内容事实，不提前集中询问、不等待回复，也不把它设为开发开工条件；由Website Manager或业务方在实际录入、审核或发布时填充。
- 典型的“血肉”包括正式商品名称、品牌归属、SKU值、分类归属、规格值、合法组合、价格、库存、图片、文案和素材授权。开发者负责让系统能够安全、动态地承载这些内容，不负责逐项代填或替业务方判断。
- 未知业务内容可使用明确标记的TEST数据或更合适的代表样本验证骨架；不得把推测值发布为正式内容。
- 只有未知信息会改变或阻塞数据结构、权限、安全、URL、SEO索引、支付、税费、物流、订单、缓存、外部集成、合规边界、迁移/回滚方案，或会造成明显结构性返工时，才向用户提出最小必要问题，并说明不确认的具体影响和最晚节点。
- 真实样本资料不足可以影响内容验收或里程碑，但不得笼统表述为“阻塞开发”；必须区分“骨架可继续”和“真实内容尚不能验收”。

## 技术约束

- 商品使用 WooCommerce 原生 Product，不创建重复商品 CPT。
- 分类使用 Product Categories；可筛选规格使用全局 Attributes；影响价格或库存的选项使用 Variations。
- ACF 只用于原生字段不足且主要用于展示的结构化数据。
- 独立内容只有在存在大量条目、独立归档/详情或筛选需求时才使用 CPT。
- 使用子主题、WooCommerce 模板覆盖、hooks 和 filters；禁止改核心文件。
- PHP 输出必须转义；输入必须清洗和验证；后台动作检查 capability 与 nonce；SQL 使用预处理。
- 保持插件数量克制，记录插件用途、许可证、更新风险和替代方案。
- 前端采用移动优先，至少验证手机、平板竖屏、平板横屏、PC。

## 最小实现、插件边界与SEO/性能优先规则

- 每个新需求先判断是否可由用户通过WordPress、WooCommerce或既有插件的原生后台安全完成。低频、可逆、不要求跨环境一致、无需审计和自动化的网站操作，优先由用户亲自执行，Codex负责解释步骤、风险和验证方法；除非原生能力不足，不为方便一次操作而新增代码。
- 当操作涉及重复执行、多人一致性、Local/Staging/Production同步、版本控制、权限或安全边界、数据完整性、批量处理、自动化、回滚，或人工操作容易产生不可接受的SEO与业务风险时，才优先代码化。不能以“用户可以手工操作”为由放弃必要的系统约束。
- 新增代码前按顺序评估：原生后台或配置能否完成；既有项目能力能否复用；是否只需在现有模块中增加最小实现；是否确有必要建立独立插件；是否必须引入新的第三方插件或外部服务。没有明确收益的抽象层、兼容层、字段、Hook、后台页面和依赖不得预先创建。
- 不把所有插件级功能机械塞入`dentall-core`。与DentAll站点核心规则强相关、应随站点长期启用、生命周期一致且规模可控的跨主题能力，放入`dentall-core`并按职责拆分内部模块；具备独立启停或部署周期、独立许可证/凭据/外部依赖、较大业务领域、可单独复用、需要故障隔离，或未来可能被替换的能力，应建立独立项目插件或采用经评估的第三方插件。
- 不为了文件数量好看而创建大量微型插件。多个插件本身不必然比一个插件慢；性能取决于每次请求实际执行的PHP、Hook、数据库查询、自动加载选项、远程请求、Cron、REST/AJAX、前端CSS/JS和缓存行为。少量职责清晰的轻量插件通常比一个无边界的大插件更易维护和隔离风险，但拆分收益不足时仍保留为现有插件内部模块。
- `dentall-core`主入口只负责插件声明、共享常量和模块加载；新增功能进入职责明确的模块，不将完整功能重新堆回主文件。只有必须始终启用、极小且不依赖普通插件启用顺序的环境或安全逻辑才进入mu-plugin。
- SEO与加载效率是所有实现、插件选型和后台配置的默认质量门槛，但不能覆盖安全、数据正确性、交易一致性、可访问性和可回滚性。不得采用关键词堆砌、隐藏内容、批量制造低质量页面或其他短期排名投机做法。
- 新增或更换插件、模块和前端资源时，必须说明并按风险验证：影响哪些请求和页面；是否增加查询、远程调用、Cron、自动加载数据或前端资源；资源能否按页面条件加载；是否改变Title、Meta、Canonical、Schema、robots、sitemap、Slug、状态码或内部链接；缓存和停用/回滚方式是什么。不能仅用“插件数量”或主观体感判断性能和SEO影响。
- 没有重构前后或变更前后的实际测量时，不得宣称性能“零影响”或排名一定提升。开发阶段至少保留可重复的页面、查询和输出基线；上线前按计划验证Core Web Vitals、真实页面缓存、错误日志、SEO输出与关键商城流程。

## Codex 代码生成硬规则

- 开始编码前必须读取 `project-docs/CODEX_WP_WC_RULES.md`，并检查目标目录中的现有实现；不得凭空假设主题结构、插件状态、WordPress、WooCommerce 或 PHP 版本。
- 每次只实现已授权范围内的最小可验证改动。不得顺手重构无关代码、安装插件、修改数据库、生产配置、真实支付、DNS 或缓存策略。
- 展示代码放子主题；跨主题且与DentAll核心生命周期一致的业务逻辑放入按职责拆分的`dentall-core`模块，满足独立生命周期或故障隔离门槛的能力使用独立插件；必须常驻且极小的逻辑才放mu-plugin。禁止把PHP、CSS、JS长期堆进后台“自定义代码”或页面编辑器。
- 禁止修改 WordPress、WooCommerce、父主题和第三方插件核心文件；优先使用 hooks、filters、原生 API，只有无法通过扩展点实现时才覆盖模板。
- 默认禁止在 PHP 模板或 HTML 中编写行内 `<style>`、`style=""`、`<script>` 和行内事件属性。静态资源必须通过 `wp_enqueue_style()`、`wp_enqueue_script()` 加载；确有运行时动态值时，使用 WordPress 提供的受控接口并说明原因。
- PHP 必须遵循“输入先清洗与验证、处理过程检查权限与 nonce、输出时按上下文转义”；nonce 不能代替 capability 检查。
- WooCommerce 商品、订单、客户和库存使用 WooCommerce CRUD/API，不直接读写内部表或依赖订单 `post/postmeta` 结构；实现必须兼容 HPOS。
- 第一版前台语言为英语、币种为美元；代码不得把英语文案、货币符号、金额格式或区域规则散落硬编码。展示文案使用WordPress国际化函数，金额和币种使用WooCommerce API，为后续多语言、多币种评估保留扩展点，但第一版不安装或实现多语言、多币种功能。
- 所有前端改动必须覆盖正常、加载、空、错误、缺图、长文本和售罄/不可购买等适用状态，并验证 390、768、1024、1440px；不得只保证单一截图宽度。
- HTML 必须语义化且可键盘操作；表单具备可见标签和明确错误提示；交互具备 focus 状态，不以颜色作为唯一信息；图片应声明尺寸或稳定宽高比，避免布局偏移。
- 新增或修改的代码，凡需要解释业务意图、安全边界、兼容性或非显而易见取舍的注释，默认使用简体中文；函数名、API 名称、协议字段和必要的原文标识保持原样。
- Git 提交记录默认使用简体中文，提交主题应直接说明变更结果，例如 `新增：添加商品卡组件`、`修复：修正购物车空状态`、`文档：补充发布流程`；不得使用含糊的 `update`、`fix stuff` 等描述。
- 完成时必须报告修改文件、关键取舍、实际验证命令/页面、未验证项，以及对数据、URL、SEO、缓存、支付、物流和部署的影响；无测试证据不得宣称完成。

## Codex 多 Agent 协作

- 多 Agent 采用“默认1个主Agent + 达到门槛才临时启动专项Agent”，不按天创建Agent，也不维持固定数量的常驻Agent；详细规则见 `project-docs/AGENT_COLLABORATION_RULES.md`。
- 用户每天说明当前D日、完成、进行中、阻塞和下一步后，主Agent必须对照总计划与项目状态判断风险。简单任务默认由主Agent处理；只有触发专项Agent门槛时才说明启动角色与职责。
- 中风险任务仅在跨文件、公共行为、WooCommerce模板覆盖或难以充分验证等门槛触发时增加Code Review Agent；高风险代码增加安全Agent；价格、库存、购物车、结账、订单、支付、退款、数据迁移和生产部署增加独立测试Agent。
- 设计稿还原、四端响应式和关键交互按需启动设计还原 Agent；需求、数据模型、URL或范围存在歧义时先启动项目/需求 Agent。
- P0/P1问题未关闭不得标记Done；P2延期必须记录原因、负责人和计划。没有可用多 Agent 调度能力时，必须明确降级方案，不得把同一轮自检称为独立Review。

## 设计与素材

- 当前首页视觉基线存放在本目录的最终效果图中。
- 结构与组件先冻结，产品图、Banner 和运营素材可使用固定比例占位图开发。
- 共用规范优先于逐页硬编码：颜色、字体、容器、间距、圆角、按钮、表单、商品卡、提示状态。
- 如果设计稿改变导航层级、字段、商品卡内容、筛选方式、购买流程或断点，视为范围变化。

## 环境与上线

- 本地、Staging、Production 分离。
- 第一周只开放受保护并禁止搜索引擎索引的 Staging 后台；这不是正式生产上线。
- D6开始安全试录，D18形成商品模型候选冻结，D24完成内容样本，D25综合验收后才开放商品、文章和页面批量录入。
- 未经明确确认，不启用真实支付、不切换 DNS、不修改生产数据。
- 上线前必须完成备份、回滚、测试订单、邮件、支付、物流、SEO、缓存和安全检查。

## 工作节奏

- 标准工作日：7 个 50 分钟专注周期，每周期后休息 10 分钟；最后 1 小时总结复盘并生成 Obsidian 笔记。
- D1～D120同时是结果驱动的计划检查点，不要求为了凑满计划工时而延迟已通过验收的任务。实际工时由用户按需记录，不作为推进门槛；同一自然日期可完成多个检查点，但每个检查点仍须满足授权范围、落地证据和验收边界。用户发现实际速度慢于预期时主动说明，再调整排期或低优先级范围。
- 每天开始明确最多 3 个验收结果。
- 当用户提出“开始DayX”或进入新的项目工作日时，默认先只读梳理，不直接实施。必须先向用户讲清当天在总计划中的目标、当前事实与前置输入、最多3个验收结果、7个专注周期、开发与业务责任边界、风险与专项Agent门槛、明确不做事项、对后续工作日的衔接，以及需要用户决定的问题；等待用户明确确认后，才修改文档、代码、网站配置或数据库。
- 每日梳理阶段形成的分类、字段、URL、技术方案或排期均标记为候选，不得仅因已写入分析文档就表述为冻结、配置完成或当日实施完成。用户确认实施后，再按授权范围执行、验证并更新正式项目状态。
- C1-C2 做最高风险工作，C3-C5 做主体实现，C6-C7 测试和修复。
- 收工前更新 `project-docs/PROJECT_STATE.md`，并按 `DentAll商城项目总计划-单休版.md` 核对状态、验收证据和偏差；实际工时仅在用户选择记录或出现进度偏差时纳入复盘。
- D1～D120每日笔记和当日形成的操作手册统一创建在 `project-docs/笔记/`，文件名采用 `Day{天数}-中文主题.md`；新增“相关笔记”章节并使用 Obsidian `[[Wiki链接]]`与直接相关笔记建立显式双向链接，不得再散落到 `project-docs/` 根目录。
- D1～D120每篇每日笔记和当日操作手册必须在文件末尾增加`## 可复用核心思想`，提炼当天可迁移到其他WordPress、WooCommerce、独立站、服务器、DevOps、安全、数据治理或项目交付工作的判断原则。不得只重复操作记录；应说明因果、边界或常见误区。
- 长期能力目标是形成Shopify＋WooCommerce双平台独立站开发能力。提炼核心思想时优先区分三层：跨平台不变量、WooCommerce/WordPress当前实现、Shopify或其他平台可能采用的对应机制；不得把某个平台的默认值、术语或插件做法误写成行业通则，也不得在未经验证时虚构一一对应关系。
- Shopify对照属于知识迁移和能力建设，不自动扩大DentAll第一版实施范围。只有与当天主题直接相关且事实可靠时才记录对照；未实际验证的Shopify机制必须标明“待验证”，未经用户明确授权不得为本项目安装、接入或实施Shopify能力。
- 每周第6天做周验收、编辑反馈收敛、风险更新和下周准备；第7天完整休息，不默认补进度。

## 完成标准

- 代码或配置已落地，不只是说明。
- 已按风险运行测试并保留证据。
- 手机、平板和 PC 关键页面无明显布局问题。
- 后台编辑流程可由非技术人员完成。
- 影响 SEO、缓存、支付或部署的变更已记录。
